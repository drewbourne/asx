package asx.number {  

  /**
   * Compares two numbers using the &lt; less than operator
   *  
   * @param n Number 
   * @param nn Number
   * @return n &lt; nn
   */
  public function lt(n:Number, nn:Number):Boolean {
    return n < nn;
  }
}
