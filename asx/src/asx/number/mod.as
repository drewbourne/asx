package asx.number {  

  /**
   * Gets the remainder of dividing a number by another
   *  
   * @param n Number
   * @param divisor Number
   * @return n % divisor
   */
  public function mod(n:Number, divisor:Number):Number {
    return n % divisor;
  } 
}
