package asx.number {  
  
  /**
   * Divides a number by another
   *  
   * @param n Number
   * @param divisor Number
   * @result n / divisor
   */
  public function div(n:Number, divisor:Number):Number {
    return n / divisor;
  }
}
