package asx.number {  

  /**
   * Compares two numbers using the > greater than operator
   *  
   * @param n Number 
   * @param nn Number
   * @return n > nn
   */
  public function gt(n:Number, nn:Number):Boolean {
    return n > nn;
  }
}
