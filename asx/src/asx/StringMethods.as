package asx
{
  public class StringMethods
  {
    // table:
    // || header || header ||
    // | field | field |
    // result:
    // [[ "header", "header" ],
    //  [ "field",  "field"  ]]
  }
}