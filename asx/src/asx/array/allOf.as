package asx.array
{
	/**
	 * @copy asx.array#every()
	 * 
	 * Alias of asx.array#every()
	 * 
	 * @see asx.array#every()
	 */
	public function allOf(iterable:Object, predicate:Function):Boolean {
		return every(iterable, predicate);
	}
}