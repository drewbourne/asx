package asx.array
{
	/**
	 * @copy asx.array#some()
	 * 
	 * Alias of asx.array#some()
	 * 
	 * @see asx.array#some()
	 */
	public function anyOf(iterable:Object, predicate:Function):Boolean {
		return some(iterable, predicate);
	}
}